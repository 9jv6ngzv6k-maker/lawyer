# Публикация статического сайта на GitHub Pages.
# Сборки нет: сайт лежит в корне репозитория. В публикацию копируются только файлы сайта;
# служебное (агенты, docs/, варианты дизайна, исходный артефакт, tools/) наружу не попадает.
# Включить: Settings → Pages → Build and deployment → Source: GitHub Actions.
name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Собрать папку публикации
        run: |
          mkdir _site
          # Публикуются только файлы сайта (белый список). Новую страницу — добавить сюда.
          cp -r index.html 404.html assets favicon.svg apple-touch-icon.png \
                site.webmanifest robots.txt sitemap.xml .nojekyll _site/
          ls -la _site

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
